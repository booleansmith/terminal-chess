name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-project:
    name: Build Terminal Chess
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Project
        uses: actions/checkout@v4.1.7
      - name: Generate CMake build files
        uses: threeal/cmake-action@v2.0.0
      - name: Run make
        run: make
      - name: Run Project
        run: ./build/terminal-chess
